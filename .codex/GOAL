✅ F3 GATE MET (automated) 2026-07-18 + SAR-005/006/007 VISUAL DoD CLOSED (D-041). The moat's trust invariant is proven keyless AND the built thin-spine UI is screenshot-verified. The next gate is the demo-spine fan-out + the Phase-1 sellable wrap.

State: SAR-001–SAR-004 (capture moat) ACCEPTED; SAR-005 (token shell + Today spine) + SAR-006 (Health slice + capture-sheet hero) + SAR-007 (F3 fake-stack eval) LANDED and now VISUAL-DoD-CLOSED. The full F3 loop runs keyless end-to-end (parse → route-by-confidence → filed-automatically strip + estimate deck → typed write via the real commit service → XP + level-up → 5-min undo → Today `via capture`), SAR-007's eval hard-asserts `wrongSilentWrites === 0` across the four gate-3 fixtures (canonical-cross-domain, estimated-meal-photo, ambiguous-skill, undo-batch), and the built surfaces are screenshot-verified at 390px + desktop (Playwright, `.verify/screens/`) with a Sol design review (5 mobile-blocking fixes landed). Validated: `pnpm check` 106 tests, build, invariants, boundary; keyless. All commits stack on branch `sar-003-schema-repository`.

STANDING GATE (D-041): a SAR with a screen/state is not complete until its built surfaces are screenshot-verified — `pnpm screenshots` (Playwright headless Chromium) at 390px + desktop → Sol (the `reviewer` agent) design-reviews the rendered shots against DESIGN §§1/4/9/11 → fixes land → re-shoot. Mobile 390px deviations block; desktop blocks only correctness (D-034). Applies to SAR-008+.

✅ **DEMO-SPINE FAN-OUT COMPLETE 2026-07-18** — SAR-008 (Money, D-042) · SAR-009 (Habits, D-043) · SAR-010 (Skills, D-044) all LANDED, gated (Sol code + D-041 design review, keyless), committed. All four domain lenses (Health · Money · Habits · Skills) are real and screenshot-verified; the last Today placeholder is retired.

NEXT (autonomous): the F3→fan-out lane continues — **SAR-011 (Meal/receipt vision path)** → SAR-012 (Onboarding + plan creation) → SAR-013 (Voice PTT + transcript safety) → SAR-014 (Coach engine, adaptations + game mechanics). All buildable keyless on the fake stack. Per-ticket flow: architect plan → Terra build → Sol code review + `pnpm check`/invariants → D-041 screenshot design review → commit. HARD STOP before SAR-021 (outer-ring: live deploy / auth / billing / secrets). Then the Phase-1 sellable wrap (docs/product/PHASES.md): live deploy → Supabase Auth + userId scoping → landing/pricing → billing rail → judge seed. Carry-forward follow-ups: F4a/F4b (correction/backdate) + question-card UI need a non-canonical fixture; UTC→user-local `localDate`; the full 12-fixture / provider-A/B EvalReport is SAR-020; the full 6-mode P11 token sweep is SAR-019.

Invariants: nothing estimated writes unconfirmed (server-enforced at the capture seam, D-040); integer units; provider-blind keyless core; repository-bound userId scoping; tokens-only UI (amber only on XP/streak/level, and only when earned — D-041); typed per-domain writes; no new domain concepts mid-build; never paywall the demo path.
