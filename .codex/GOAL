CURRENT GATE — FLOWS F3 end-to-end on the fake stack, keyless, with Health.

State: SAR-001 (scaffold) · SAR-002 (provider/fake stack) · SAR-003 (dialect schema + bound repository factory + local auth) · SAR-004 (capture routing → typed commit → XP → 5-min undo — the moat) are ACCEPTED. The moat is built and validated headless + keyless (pnpm check 85 tests, build, invariants clean) and gate-reviewed two passes (one blocking finding — backdate streak recompute — fixed; re-review ACCEPTABLE TO LAND). SAR-004 stacks uncommitted on branch `sar-003-schema-repository` (SAR-003 committed there) pending Satvik's branch/commit/merge call.

NEXT: SAR-005 (token shell + thin Today spine) → SAR-006 (Health slice + capture sheet) → SAR-007 (F3 fake-stack eval). SAR-004 delivers the middle of F3 (parse → route → typed commit → XP → undo); the F3 FLOWS gate itself passes at SAR-007 once the UI lands. F3 is the Day-1 acceptance gate and must pass keyless before fan-out.

Invariants: nothing estimated writes unconfirmed; integer units (paise/ml/minutes/grams); provider-blind keyless core; repository-bound userId scoping (callers never pass userId); tokens-only UI; typed per-domain writes; no new domain concepts mid-build; never paywall the demo path.
