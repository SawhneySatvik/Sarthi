CURRENT GATE — FLOWS F3 end-to-end on the fake stack, keyless, with Health. SAR-006 makes F3 RUNNABLE; SAR-007 formally EVALS it (the gate passes when SAR-007 is green).

State: SAR-001–SAR-004 (through the capture moat) are ACCEPTED. SAR-005 (token shell + thin Today spine) and SAR-006 (Health slice + capture-sheet hero — the F3 loop) are LANDED (code). The full F3 loop runs keyless: hold-mic/type → parse → route-by-confidence → filed-automatically strip + estimate deck (4 gestures) → typed Health write via the real commit service → XP + inline level-up → 5-min undo → Today shows `via capture`; plus the Health lens (3 rings). Validated: pnpm check 101 tests, build, invariants, boundary; route-loop + render smokes; two-pass Sol review ACCEPTABLE TO LAND (4 blocking findings fixed). The server RE-ROUTES the auto batch (D-040) — no estimated value writes without an explicit accept, even from a hostile client; wrongSilentWrites stays 0. ONE open DoD before SAR-006 CLOSES: visual screenshot-verify at 390px + desktop (Satvik's local pass — `pnpm db:seed:dev && pnpm dev`). All commits stack on branch `sar-003-schema-repository`.

NEXT: SAR-007 (F3 fake-stack integration eval — the GATE; `wrongSilentWrites === 0` hard assertion + F3 state screenshots). Then fan-out (SAR-008 Money / SAR-009 Habits / SAR-010 Skills) and the Phase-1 sellable wrap. Carry into SAR-007+: F4a/F4b (correction/backdate) + question-card UI need a non-canonical fixture; UTC→user-local `localDate`.

Invariants: nothing estimated writes unconfirmed (now enforced server-side at the capture seam too); integer units; provider-blind keyless core; repository-bound userId scoping; tokens-only UI (amber only on XP/streak/level); typed per-domain writes; no new domain concepts mid-build; never paywall the demo path.
