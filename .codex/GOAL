CURRENT GATE — FLOWS F3 end-to-end on the fake stack, keyless, with Health.

State: SAR-001 (scaffold) · SAR-002 (provider/fake stack) · SAR-003 (dialect schema + bound repository factory + local auth) · SAR-004 (capture routing → typed commit → XP → 5-min undo — the moat) are ACCEPTED. SAR-005 (token shell + thin Today spine) is LANDED (code): 6 theme-modes, 5-tab shell + static capture bar, and a Today spine reading real repos via a server-only accessor + a pure framework-clean read-model. Validated keyless (pnpm check 94 tests, build, invariants, compiled-CSS + AA contrast + runtime smoke) and Sol-reviewed ACCEPTABLE TO LAND (0 blocking). ONE open DoD before SAR-005 is CLOSED: visual screenshot-verify at 390px + desktop (Satvik's local pass — `pnpm db:seed:dev && pnpm dev`). All commits stack on branch `sar-003-schema-repository`.

NEXT: SAR-006 (Health slice + capture sheet — the hero swipe deck + live commit-driven auto-check) → SAR-007 (F3 fake-stack eval). SAR-005+006 make the SAR-004 pipeline visible; SAR-007 closes the F3 gate. Carry into SAR-006: UTC→user-local `localDate` at the capture edge.

Invariants: nothing estimated writes unconfirmed; integer units (paise/ml/minutes/grams); provider-blind keyless core; repository-bound userId scoping (callers never pass userId); tokens-only UI (amber only on XP/streak/level); typed per-domain writes; no new domain concepts mid-build; never paywall the demo path.
