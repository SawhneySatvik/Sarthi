✅ F3 GATE MET (automated) 2026-07-18 — the moat's trust invariant is proven keyless. The next gate is the demo-spine fan-out + the Phase-1 sellable wrap.

State: SAR-001–SAR-004 (capture moat) ACCEPTED; SAR-005 (token shell + Today spine) + SAR-006 (Health slice + capture-sheet hero) + SAR-007 (F3 fake-stack eval) LANDED. The full F3 loop runs keyless end-to-end (parse → route-by-confidence → filed-automatically strip + estimate deck → typed write via the real commit service → XP + level-up → 5-min undo → Today `via capture`), and SAR-007's eval hard-asserts `wrongSilentWrites === 0` across the four Architecture gate-3 fixtures (canonical-cross-domain, estimated-meal-photo, ambiguous-skill, undo-batch). Validated: `pnpm check` 106 tests, build, invariants, boundary; keyless. All commits stack on branch `sar-003-schema-repository`. Open manual DoD across SAR-005/006/007: visual screenshot-verify at 390px + desktop (no browser tooling in the repo — Satvik's local pass / a browser-tooled session).

NEXT: the demo-spine fan-out — **SAR-008 (Money typed store + ledger lens)** → SAR-009 (Habits + satisfied-by grid) → SAR-010 (Skills mastery lens) — each is "a schema already exists + a lens" over the built pipeline. Then the Phase-1 sellable wrap (docs/product/PHASES.md): live deploy → Supabase Auth + userId scoping → landing/pricing → billing rail → judge seed. Carry-forward follow-ups: F4a/F4b (correction/backdate) + question-card UI need a non-canonical fixture; UTC→user-local `localDate`; the full 12-fixture / provider-A/B EvalReport is SAR-020.

Invariants: nothing estimated writes unconfirmed (server-enforced at the capture seam, D-040); integer units; provider-blind keyless core; repository-bound userId scoping; tokens-only UI (amber only on XP/streak/level); typed per-domain writes; no new domain concepts mid-build; never paywall the demo path.
