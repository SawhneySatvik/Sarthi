✅ SAR-016/017 (Tools loops + Settings) LANDED 2026-07-19. Tools replaces its stub with explicit, idempotent Focus and Meditation command loops over the typed commit/undo seam: Focus writes one `skillSession` with `source:'timer'`; consented Meditation writes one `habitLog` with `source:'tool'` and atomically creates/removes first-use `Meditate`. Starts, discards, suggestions, and coming-soon cards write nothing. Settings now opens from the avatar as a quiet sheet with real token previews, local preferences, scoped JSON export, an honest unavailable danger zone, and a developer-only, request-scoped LLM override that public production ignores. `pnpm check` 221 green; build, invariants, diff check, `timer-session` eval, and the D-041 screenshot review pass keyless.

State: SAR-001–SAR-017 accepted/landed. All four domain lenses, vision, onboarding, safe PTT voice capture, Coach/game backend, Tools, and Settings are real. All commits stack on branch `sar-003-schema-repository`.

STANDING GATE (D-041): a SAR with a screen/state is screenshot-verified — `pnpm screenshots` (Playwright headless Chromium, 390 + desktop) → Sol (`reviewer`) design review of the rendered shots → fixes → re-shoot. Mobile 390 deviations block; desktop blocks only correctness (D-034). (SAR-012 Pass 3 was closed without the final re-shoot at the human's call — see D-046; the two Pass-3 polish fixes are pnpm-check-green but un-re-screenshotted.)

NEXT: **SAR-018 (Flow stitch and domain-complete integration)** proves F2–F11 can run end-to-end against real scoped repositories on the keyless fake stack, repairing only integration seams. Per-ticket flow: architect plan → Terra build → Sol code review + `pnpm check`/invariants → D-041 screenshot design review → commit. HARD STOP before SAR-021 (outer-ring: live deploy / auth / billing / secrets). Carry-forward follow-ups: E-section voice-fill parity (deferred); the seed-route guard must assert `AUTH_PROVIDER=supabase` in SAR-021's deploy checklist; SAR-012 Pass-3's two polish fixes are un-re-screenshotted (trivial); the full 12-fixture / provider-A/B EvalReport is SAR-020; the full 6-mode P11 token sweep is SAR-019.

Invariants: nothing estimated writes unconfirmed (server-enforced at the capture seam, D-040); integer units; provider-blind keyless core; repository-bound userId scoping; tokens-only UI (amber only when earned — D-041; the A→D onboarding hairline is the sanctioned SCREEN-ONBOARDING §1 exception); typed per-domain writes; no new domain concepts mid-build; never paywall the demo path.
