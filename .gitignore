# Dependencies and package-manager state.
node_modules/
.pnpm-store/
pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Framework and build output.
.next/
out/
coverage/
*.tsbuildinfo

# Local deployment state and operating-system metadata.
.vercel/
.DS_Store

# Local runtime configuration; .env.example remains tracked.
.env
.env.*
!.env.example

# Local SQLite state.
*.db
*.db-journal
*.sqlite
*.sqlite3

# Local screenshot-verification evidence (D-041); not shipped to the remote.
.verify/

# Raw source art (PNG originals); optimized public/art/*.webp derivatives stay tracked.
assets/

# Local mirror layer — parallel to .codex/, kept out of the tracked provenance surface.
.claude/
CLAUDE.md
claude.mk
