/** Framework-clean contracts shared by core use cases and adapter implementations. */
export type {
  ImageInput,
  LlmGateway,
  LlmProviderName,
  ObjectRequest,
  ObjectResult,
  TextRequest,
  Tier,
  Transcription,
  VisionProvider,
  VisionProviderName,
  VoiceAudio,
  VoiceProvider,
  VoiceProviderName,
} from "./providers";
