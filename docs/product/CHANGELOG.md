# docs/product/CHANGELOG.md — Sarthi Build Provenance

## 2026-07-17 — Session 1 kickoff

- **DOC SET FINALIZED 2026-07-17**
- Codex `/feedback` session ID: `019f6cc9-957e-7ae3-8ffa-c54fc699eb44`
- Codex accelerated the kickoff environment audit: verified repository hooks, profiles, provenance tracking, and build gates before implementation.
- Codex accelerated the document lock: cross-indexed the D-026–D-029 drift, resolved trust/auth/billing decisions into D-030–D-034, and reconciled the fake local track with the production sellable rail.
- Codex accelerated session continuity: reorganized the doc set by product, architecture, experience, screens, planning, operations, and roadmap; updated every build-harness and internal reference.
- Codex accelerated the architecture lock: independently verified the runtime model matrix and AI SDK structured-output constraints before the provider contract was frozen.
- **ARCHITECTURE LOCKED 2026-07-17** — `docs/architecture/ARCHITECTURE.md` is now the signed build blueprint.
- **D-035 LOCKED 2026-07-17** — execution advances on dependency and acceptance gates, never elapsed-day checkpoints; the external submission deadline remains fixed.
- **TICKETS LOCKED 2026-07-17** — `docs/planning/TICKETS.md` is the approved Phase-0/1 execution register; build is intentionally deferred before `SAR-001`.
- **SAR-001 SCAFFOLD ACCEPTED 2026-07-17** — Codex accelerated the framework-clean foundation: created the keyless typed runtime selector and AST-enforced import boundary with forbidden-import fixtures; frozen install, check, production build, and final gate review passed.
- **D-036 LOCKED 2026-07-17** — Codex accelerated runtime wiring safety: verified an ABI conflict in the original provider pins, aligned the full AI SDK 7 package set, and preserved every locked runtime model ID before SAR-002 implementation.
- **SAR-002 PROVIDER PORTS ACCEPTED 2026-07-17** — Codex accelerated the provider-safe fake stack: added framework-clean LLM/voice/vision ports, immutable verified-model routing, and deterministic no-key/no-network fixtures with schema-validation coverage. The ambiguous “bottle” fixture remains an explicitly unknown, estimated pending value rather than a silent quantity; frozen install, checks, build, invariant validation, and final gate review passed.
- **SAR-003 SCHEMA + REPOSITORY FACTORY ACCEPTED 2026-07-17** — Codex accelerated the typed persistence floor: authored the drizzle-free shared schema contract (33 tables), the full SQLite and Postgres Drizzle dialects, a descriptor-driven user-scoped `RepositoryFactory.forUser()` with transactional commit/rollback, and the keyless `local-password` auth gate (constant-time compare → `local-dev`; Supabase left composition-ready). Every persistent row carries a non-null `userId`; no repository method accepts a caller-supplied `userId`, arbitrary SQL, or tenant filter — enforced at the type surface (`Omit<…,'userId'>`) and re-injected at runtime from the bound scope; money stays integer paise with non-negative bounds. `core/` stays drizzle/framework-import-clean; the shared contract stays drizzle-free (test-guarded). The committed SQLite migration applies keylessly, dual-dialect parity and cross-tenant isolation are covered, and frozen install, `pnpm check` (46 tests), build, invariant validation, and a two-pass final gate review (one blocking finding fixed) all passed. Deferred non-blocking follow-ups recorded in the handoff.
- Codex accelerated the moat's trust proof: the adversarial gate review independently caught a **backdate streak-recompute deviation** from the signed plan (the incremental streak form would have regressed `lastActiveDate` and inflated a streak across a backdated day), and drove the grace-aware `computeStreak`-over-logs fix plus a test that encodes the exact inflation scenario — a wrong user-visible number stopped before the pipeline landed.
- **SAR-004 CAPTURE + TYPED COMMIT + XP + UNDO ACCEPTED 2026-07-17** — Codex accelerated the moat (the one capture pipeline every domain reuses), keyless and headless: a versioned draft-permissive / resolved-strict `CaptureDraft` contract (7 kinds, gate 9.2); a route-by-confidence policy whose `AUTO_WRITE_CONFIDENCE_BPS = 9000` lives in exactly one file (auto only when `create` + `estimated:false` + `≥9000` bps + integer-valid + name→entity resolved + unblocked — everything else pending); name→entity resolution that demotes auto→pending on failure (never invents a habit/skill/category); one transactional commit batch through an **exhaustive typed per-domain switch** (no generic payload/table path) writing envelope + typed rows + `commit_rows` snapshots + deterministic integer XP/streak + plan and satisfied-by effects, with idempotent replay and per-scope single-flight; and a five-minute latest-batch undo that reverses every side effect atomically via the row snapshots. The resolved-strict schema now runs at commit entry, so a null/float/unresolved value is rejected before any write. `wrongSilentWrites === 0` holds across the capture suites; the whole loop runs on the `fake` stack with keys unset and `fetch` sentineled. Frozen install, `pnpm check` (**85 tests**), build, invariant validation, and a **two-pass Sol gate review** (one blocking finding — the backdate streak recompute — fixed and re-reviewed ACCEPTABLE TO LAND) all passed. This is Architecture build-gate 9.2 and the safety half of 9.3; it makes the F3 gate (SAR-005/006/007) winnable. Deferred non-blocking follow-ups recorded in the handoff.
- Codex accelerated the presentation floor's trust boundary: the gate review verified the six-theme token layer actually *compiles* (Tailwind silently drops unknown utilities — a headless grep of the emitted CSS confirmed every token utility + all six `[data-theme][data-mode]` blocks), that the pure Today read-model stays framework-import-clean, and that the Done/Skip write is a user-scoped manual tap no foreign id can reach — before the shell landed.
- **SAR-005 TOKEN SHELL + THIN TODAY SPINE LANDED 2026-07-18** — Codex accelerated the presentation floor: a full CSS-variable token system (**3 themes × 2 modes** via `[data-theme][data-mode]`, CI-guarded 6-block parity, self-hosted `next/font`, no hardcoded colour/radius/duration in any component, `--energy` amber only on XP/streak/level, WCAG-AA ink contrast verified headlessly across all six modes), the app shell (5-tab nav + header + a static global capture bar + 4 route stubs + a dev theme-cycle pill), and a thin Today spine that reads the real SAR-003 repositories through a new **server-only composition accessor** and a **pure framework-clean read-model** (`core/domains/today.ts`) — rendering the amber stat cluster, coach line, domain switcher (placeholder lenses), and the NEXT UP / LATER / COMPLETED plan spine with `via capture` provenance. Done/Skip is a real **user-scoped manual write** (`completionSource:'manual'`), never an estimate, and never the capture/commit path. Frozen install, `pnpm check` (**94 tests**), build, and invariant validation pass keyless; a runtime smoke (`next start` + seeded SQLite) rendered real end-to-end data with no server errors; a **Sol gate review returned ACCEPTABLE TO LAND (0 blocking)**. This is Architecture P0 + P2 and the shell half of F3. **Open DoD item before the ticket is CLOSED: visual screenshot-verify at 390px + desktop.** Recorded cuts + deferred follow-ups (UTC→local `localDate`, header time-of-day scene, satisfied-by rendering) are in D-039 + the handoff.
- Codex accelerated the moat's UI trust boundary: a two-pass adversarial gate review caught that the capture sheet's *happy-path* logic broke ask-don't-invent on the *failure* path — a proposal that failed resolution could vanish or show as "filed automatically" though unwritten, and the commit route persisted the SILENT auto batch as `status:'accepted'` (trusting the client's partition, so a wrong silent write would be invisible to SAR-007's eval). Both were fixed before landing: the strip now shows only committed rows, refused proposals re-deck as questions, and the server re-routes the auto batch itself (D-040) — no estimated value is written without an explicit accept, even from a hostile client.
- **SAR-006 HEALTH SLICE + CAPTURE-SHEET HERO (F3) LANDED 2026-07-18** — Codex accelerated the hero and the F3 loop: the capture sheet wires the SAR-004 pipeline (parse → route → resolve → commit → undo) to the SAR-005 shell through keyless route handlers (`app/api/capture/{parse,commit,undo}`, D-040). Hold-mic/type → parse (fake gateway, keyless) → route-by-confidence → the **two confirm zones** (a calm "filed automatically" strip of server-committed auto rows vs. a live framer-motion estimate deck) → the four gestures (accept/discard/edit/why) → typed Health write through the **real commit service** → XP + inline level-up bloom (≤900ms, amber only here + on XP) → 5-min undo, with Today showing `via capture` on refresh. The **Health lens** (three glanceable rings — energy/water/protein — over a pure framework-clean `core/domains/health.ts`) lands in the Today domain switcher. Invariant #1 holds end-to-end **and in the UI**: the server re-routes the claimed-auto batch and refuses any estimate (proven by a runtime smoke — an estimated proposal sent as auto is 409-refused, never written; explicit accept then writes it); `wrongSilentWrites` stays 0. Frozen install, `pnpm check` (**101 tests**), build, invariant validation pass keyless; a **two-pass Sol gate review** (four blocking findings — phantom strip rows, undo-failure divergence, amber-on-orb, and the auto-batch provenance/re-route — all fixed and re-reviewed ACCEPTABLE TO LAND) plus two hardening follow-ups closed. `framer-motion` added; static Capture Orb only (shader deferred). This is **FLOWS F3 made runnable** on the fake stack; SAR-007 formally evals it next. **Open DoD before CLOSE: visual screenshot-verify** (390px + desktop, all capture states). Recorded cuts/follow-ups (F4a/F4b + the question-card UI are unreachable on the content-blind fake fixture so proven by unit tests only; mic uses the fake-voice shortcut; fan-out/undo-toast/40ms-stagger trims; habitLog toggle; one coach note per accept) are in the handoff.
- Codex accelerated the eval's honesty: pre-build research caught that D-040 (written at SAR-006 land) claimed rows persist a `status:'auto'/'accepted'` column that does not exist — so the `wrongSilentWrites` counter would have been built against a phantom field. Corrected before a line of eval code: the counter reads the persisted `estimated` column + each fixture's expected-auto allowlist (the durable auto/accepted marker is the `commits.kind` envelope), and D-040's wording was fixed.
- **SAR-007 F3 FAKE-STACK EVAL — F3 GATE MET (automated) 2026-07-18** — Codex accelerated the trust gate: a deterministic, keyless integration eval (`tests/eval/`) drives the four Architecture gate-3 fixtures through the landed F3 pipeline — `canonical-cross-domain` (through the real `parseDump` hop), `estimated-meal-photo` (synthesized keyless, no vision pipeline), `ambiguous-skill`, `undo-batch` — and makes **`wrongSilentWrites === 0` a HARD AGGREGATE** over every scenario (closing the N-6 gap SAR-004 left as per-scenario asserts). It proves: explicit high-confidence rows auto-file; every estimate / low-confidence / ambiguous proposal writes **nothing until an explicit accept**; an accepted meal card writes correctly; undo reverses rows + XP atomically. The eval reads what is actually persisted (`estimated` + the allowlist), produces a minimal `EvalReport` (a strict subset of §8.2 — the full 12-fixture / provider-A/B harness is SAR-020), and is `test-eval`-clean (no production module edited — only fixtures, the harness, the D-040 wording fix, and this entry). Frozen install, `pnpm check` (**106 tests**), build, and invariant validation pass keyless. **This is the Day-1 F3 acceptance gate met (automated); the visual F3-state screenshots (390px + desktop) remain the manual DoD remainder** (no browser tooling in the repo). A Sol review confirmed the counter is non-vacuous by mutation probe (an estimated proposal committed as auto makes it return 1). Deferred to SAR-020's full harness (Sol MEDIUMs, gate unaffected — each is enforced today by per-fixture asserts): fold the expected-auto allowlist + a `commits.kind`-based habit-write check into the `wrongSilentWrites` metric itself, and add the canonical meal-accept → meal + `mealItems` step to the gate scenario. On this gate green, fan-out (SAR-008/009/010) may begin.
