# docs/product/CHANGELOG.md — Sarthi Build Provenance

## 2026-07-17 — Session 1 kickoff

- **DOC SET FINALIZED 2026-07-17**
- Codex `/feedback` session ID: `019f6cc9-957e-7ae3-8ffa-c54fc699eb44`
- Codex accelerated the kickoff environment audit: verified repository hooks, profiles, provenance tracking, and build gates before implementation.
- Codex accelerated the document lock: cross-indexed the D-026–D-029 drift, resolved trust/auth/billing decisions into D-030–D-034, and reconciled the fake local track with the production sellable rail.
- Codex accelerated session continuity: reorganized the doc set by product, architecture, experience, screens, planning, operations, and roadmap; updated every build-harness and internal reference.
- Codex accelerated the architecture lock: independently verified the runtime model matrix and AI SDK structured-output constraints before the provider contract was frozen.
- **ARCHITECTURE LOCKED 2026-07-17** — `docs/architecture/ARCHITECTURE.md` is now the signed build blueprint.
- **D-035 LOCKED 2026-07-17** — execution advances on dependency and acceptance gates, never elapsed-day checkpoints; the external submission deadline remains fixed.
- **TICKETS LOCKED 2026-07-17** — `docs/planning/TICKETS.md` is the approved Phase-0/1 execution register; build is intentionally deferred before `SAR-001`.
- **SAR-001 SCAFFOLD ACCEPTED 2026-07-17** — Codex accelerated the framework-clean foundation: created the keyless typed runtime selector and AST-enforced import boundary with forbidden-import fixtures; frozen install, check, production build, and final gate review passed.
- **D-036 LOCKED 2026-07-17** — Codex accelerated runtime wiring safety: verified an ABI conflict in the original provider pins, aligned the full AI SDK 7 package set, and preserved every locked runtime model ID before SAR-002 implementation.
- **SAR-002 PROVIDER PORTS ACCEPTED 2026-07-17** — Codex accelerated the provider-safe fake stack: added framework-clean LLM/voice/vision ports, immutable verified-model routing, and deterministic no-key/no-network fixtures with schema-validation coverage. The ambiguous “bottle” fixture remains an explicitly unknown, estimated pending value rather than a silent quantity; frozen install, checks, build, invariant validation, and final gate review passed.
- **SAR-003 SCHEMA + REPOSITORY FACTORY ACCEPTED 2026-07-17** — Codex accelerated the typed persistence floor: authored the drizzle-free shared schema contract (33 tables), the full SQLite and Postgres Drizzle dialects, a descriptor-driven user-scoped `RepositoryFactory.forUser()` with transactional commit/rollback, and the keyless `local-password` auth gate (constant-time compare → `local-dev`; Supabase left composition-ready). Every persistent row carries a non-null `userId`; no repository method accepts a caller-supplied `userId`, arbitrary SQL, or tenant filter — enforced at the type surface (`Omit<…,'userId'>`) and re-injected at runtime from the bound scope; money stays integer paise with non-negative bounds. `core/` stays drizzle/framework-import-clean; the shared contract stays drizzle-free (test-guarded). The committed SQLite migration applies keylessly, dual-dialect parity and cross-tenant isolation are covered, and frozen install, `pnpm check` (46 tests), build, invariant validation, and a two-pass final gate review (one blocking finding fixed) all passed. Deferred non-blocking follow-ups recorded in the handoff.
- Codex accelerated the moat's trust proof: the adversarial gate review independently caught a **backdate streak-recompute deviation** from the signed plan (the incremental streak form would have regressed `lastActiveDate` and inflated a streak across a backdated day), and drove the grace-aware `computeStreak`-over-logs fix plus a test that encodes the exact inflation scenario — a wrong user-visible number stopped before the pipeline landed.
- **SAR-004 CAPTURE + TYPED COMMIT + XP + UNDO ACCEPTED 2026-07-17** — Codex accelerated the moat (the one capture pipeline every domain reuses), keyless and headless: a versioned draft-permissive / resolved-strict `CaptureDraft` contract (7 kinds, gate 9.2); a route-by-confidence policy whose `AUTO_WRITE_CONFIDENCE_BPS = 9000` lives in exactly one file (auto only when `create` + `estimated:false` + `≥9000` bps + integer-valid + name→entity resolved + unblocked — everything else pending); name→entity resolution that demotes auto→pending on failure (never invents a habit/skill/category); one transactional commit batch through an **exhaustive typed per-domain switch** (no generic payload/table path) writing envelope + typed rows + `commit_rows` snapshots + deterministic integer XP/streak + plan and satisfied-by effects, with idempotent replay and per-scope single-flight; and a five-minute latest-batch undo that reverses every side effect atomically via the row snapshots. The resolved-strict schema now runs at commit entry, so a null/float/unresolved value is rejected before any write. `wrongSilentWrites === 0` holds across the capture suites; the whole loop runs on the `fake` stack with keys unset and `fetch` sentineled. Frozen install, `pnpm check` (**85 tests**), build, invariant validation, and a **two-pass Sol gate review** (one blocking finding — the backdate streak recompute — fixed and re-reviewed ACCEPTABLE TO LAND) all passed. This is Architecture build-gate 9.2 and the safety half of 9.3; it makes the F3 gate (SAR-005/006/007) winnable. Deferred non-blocking follow-ups recorded in the handoff.
- Codex accelerated the presentation floor's trust boundary: the gate review verified the six-theme token layer actually *compiles* (Tailwind silently drops unknown utilities — a headless grep of the emitted CSS confirmed every token utility + all six `[data-theme][data-mode]` blocks), that the pure Today read-model stays framework-import-clean, and that the Done/Skip write is a user-scoped manual tap no foreign id can reach — before the shell landed.
- **SAR-005 TOKEN SHELL + THIN TODAY SPINE LANDED 2026-07-18** — Codex accelerated the presentation floor: a full CSS-variable token system (**3 themes × 2 modes** via `[data-theme][data-mode]`, CI-guarded 6-block parity, self-hosted `next/font`, no hardcoded colour/radius/duration in any component, `--energy` amber only on XP/streak/level, WCAG-AA ink contrast verified headlessly across all six modes), the app shell (5-tab nav + header + a static global capture bar + 4 route stubs + a dev theme-cycle pill), and a thin Today spine that reads the real SAR-003 repositories through a new **server-only composition accessor** and a **pure framework-clean read-model** (`core/domains/today.ts`) — rendering the amber stat cluster, coach line, domain switcher (placeholder lenses), and the NEXT UP / LATER / COMPLETED plan spine with `via capture` provenance. Done/Skip is a real **user-scoped manual write** (`completionSource:'manual'`), never an estimate, and never the capture/commit path. Frozen install, `pnpm check` (**94 tests**), build, and invariant validation pass keyless; a runtime smoke (`next start` + seeded SQLite) rendered real end-to-end data with no server errors; a **Sol gate review returned ACCEPTABLE TO LAND (0 blocking)**. This is Architecture P0 + P2 and the shell half of F3. **Open DoD item before the ticket is CLOSED: visual screenshot-verify at 390px + desktop.** Recorded cuts + deferred follow-ups (UTC→local `localDate`, header time-of-day scene, satisfied-by rendering) are in D-039 + the handoff.
- Codex accelerated the moat's UI trust boundary: a two-pass adversarial gate review caught that the capture sheet's *happy-path* logic broke ask-don't-invent on the *failure* path — a proposal that failed resolution could vanish or show as "filed automatically" though unwritten, and the commit route persisted the SILENT auto batch as `status:'accepted'` (trusting the client's partition, so a wrong silent write would be invisible to SAR-007's eval). Both were fixed before landing: the strip now shows only committed rows, refused proposals re-deck as questions, and the server re-routes the auto batch itself (D-040) — no estimated value is written without an explicit accept, even from a hostile client.
- **SAR-006 HEALTH SLICE + CAPTURE-SHEET HERO (F3) LANDED 2026-07-18** — Codex accelerated the hero and the F3 loop: the capture sheet wires the SAR-004 pipeline (parse → route → resolve → commit → undo) to the SAR-005 shell through keyless route handlers (`app/api/capture/{parse,commit,undo}`, D-040). Hold-mic/type → parse (fake gateway, keyless) → route-by-confidence → the **two confirm zones** (a calm "filed automatically" strip of server-committed auto rows vs. a live framer-motion estimate deck) → the four gestures (accept/discard/edit/why) → typed Health write through the **real commit service** → XP + inline level-up bloom (≤900ms, amber only here + on XP) → 5-min undo, with Today showing `via capture` on refresh. The **Health lens** (three glanceable rings — energy/water/protein — over a pure framework-clean `core/domains/health.ts`) lands in the Today domain switcher. Invariant #1 holds end-to-end **and in the UI**: the server re-routes the claimed-auto batch and refuses any estimate (proven by a runtime smoke — an estimated proposal sent as auto is 409-refused, never written; explicit accept then writes it); `wrongSilentWrites` stays 0. Frozen install, `pnpm check` (**101 tests**), build, invariant validation pass keyless; a **two-pass Sol gate review** (four blocking findings — phantom strip rows, undo-failure divergence, amber-on-orb, and the auto-batch provenance/re-route — all fixed and re-reviewed ACCEPTABLE TO LAND) plus two hardening follow-ups closed. `framer-motion` added; static Capture Orb only (shader deferred). This is **FLOWS F3 made runnable** on the fake stack; SAR-007 formally evals it next. **Open DoD before CLOSE: visual screenshot-verify** (390px + desktop, all capture states). Recorded cuts/follow-ups (F4a/F4b + the question-card UI are unreachable on the content-blind fake fixture so proven by unit tests only; mic uses the fake-voice shortcut; fan-out/undo-toast/40ms-stagger trims; habitLog toggle; one coach note per accept) are in the handoff.
- Codex accelerated the eval's honesty: pre-build research caught that D-040 (written at SAR-006 land) claimed rows persist a `status:'auto'/'accepted'` column that does not exist — so the `wrongSilentWrites` counter would have been built against a phantom field. Corrected before a line of eval code: the counter reads the persisted `estimated` column + each fixture's expected-auto allowlist (the durable auto/accepted marker is the `commits.kind` envelope), and D-040's wording was fixed.
- **SAR-007 F3 FAKE-STACK EVAL — F3 GATE MET (automated) 2026-07-18** — Codex accelerated the trust gate: a deterministic, keyless integration eval (`tests/eval/`) drives the four Architecture gate-3 fixtures through the landed F3 pipeline — `canonical-cross-domain` (through the real `parseDump` hop), `estimated-meal-photo` (synthesized keyless, no vision pipeline), `ambiguous-skill`, `undo-batch` — and makes **`wrongSilentWrites === 0` a HARD AGGREGATE** over every scenario (closing the N-6 gap SAR-004 left as per-scenario asserts). It proves: explicit high-confidence rows auto-file; every estimate / low-confidence / ambiguous proposal writes **nothing until an explicit accept**; an accepted meal card writes correctly; undo reverses rows + XP atomically. The eval reads what is actually persisted (`estimated` + the allowlist), produces a minimal `EvalReport` (a strict subset of §8.2 — the full 12-fixture / provider-A/B harness is SAR-020), and is `test-eval`-clean (no production module edited — only fixtures, the harness, the D-040 wording fix, and this entry). Frozen install, `pnpm check` (**106 tests**), build, and invariant validation pass keyless. **This is the Day-1 F3 acceptance gate met (automated); the visual F3-state screenshots (390px + desktop) remain the manual DoD remainder** (no browser tooling in the repo). A Sol review confirmed the counter is non-vacuous by mutation probe (an estimated proposal committed as auto makes it return 1). Deferred to SAR-020's full harness (Sol MEDIUMs, gate unaffected — each is enforced today by per-fixture asserts): fold the expected-auto allowlist + a `commits.kind`-based habit-write check into the `wrongSilentWrites` metric itself, and add the canonical meal-accept → meal + `mealItems` step to the gate scenario. On this gate green, fan-out (SAR-008/009/010) may begin.
- Codex accelerated the design bar: wired headless-Chromium screenshot tooling (Playwright · `pnpm screenshots` · `scripts/screenshot.mjs`) on the build engine, captured 38 built-surface states at 390px + desktop across the theme-modes into `.verify/screens/`, and ran a **Sol design review of the RENDERED UI** — not just the code — closing the visual DoD that SAR-005/006/007 had each deferred for lack of a browser. The pass also caught a screenshot-harness staleness bug (the module-level DB-factory memoisation serves stale data on mid-session re-seed) and proved the read-model correct by curl.
- **SAR-005/006/007 VISUAL DoD CLOSED 2026-07-18 (D-041)** — the built UI was screenshot-verified against Premium-Dark intent + the de-slop law. The Sol design review surfaced 5 mobile-blocking issues, all fixed: the **level-up bloom** was amber-on-amber illegible (now a legible label over a soft glow); **accepted estimates vanished** from the done state (now a "Confirmed by you" ledger — the trust picture balances on screen); the **Health protein/water rings** read as amber/neon and failed AA on light (now clay / slate-teal, moved per-mode); **un-earned amber** on the arc day-counter + zero/base stats (now neutral — amber only when earned); and the **dev theme-pill obscured the Settings avatar** (repositioned + hidden in shots). Plus polish: the two capture confirm zones are now categorically different (flat filed-strip rows vs. the single raised estimate card), completed rows dropped the punitive strikethrough, Health rows use real labels + a glass-box `~` on estimates. `pnpm check` (**106 tests**), build, and invariants stay green; the re-shot evidence lives in `.verify/screens/`. **Sol is now the standing reviewer for both code and design (D-041); visual verify is a real completion gate for SAR-008+.** Fan-out is next.
- **SAR-008 — Money ledger lens LANDED 2026-07-18 (D-042).** The first fan-out lens: the Money domain (typed `transactions`/`categories`/`budgets`/recurring rules the capture pipeline already writes) now renders on Today as a pure read-model (`core/domains/money.ts` `buildMoneyView`) + lens (`components/lenses/MoneyLens.tsx`) — grouped day ledger, a glass-box safe-to-spend math sheet (balance − remaining-budgeted − upcoming-recurring, honest negatives), token-status budget bars, recurring shelf, and a category drill (push/return). **Integer paise end-to-end, no float** (Sol code review: zero blocking, all §2 invariants hold). Gated the full loop: Sol code review + Sol D-041 design review of the rendered shots → **2 mobile-blocking fixes landed** (the ledger was clipped behind the fixed capture bar → opaque backdrop + the shot proves it now scrolls clear; the 95% budget bar didn't turn `--warn` because dark `--warn` == `--dom-money` → retuned) + F3 token retune (dark `--dom-money` → bronze `#c0883a`, off the `--energy` amber) + the minus-glyph unified to U+2212. `pnpm check` **119 tests** green, invariants clean, keyless; evidence in `.verify/screens/money-*`. Next: SAR-009 (Habits satisfied-by grid).
- **SAR-009 — Habits satisfied-by grid LANDED 2026-07-18 (D-043).** The second fan-out lens: Habits (`habits`/`habit_logs`/`habit_satisfaction_rules`) render as a pure read-model (`core/domains/habits.ts`) + lens with grace-aware streaks (reusing the `core/game/streak.ts` kernel via a behaviour-identical `GRACE_DAYS` lift) and a month heatmap. **The satisfied-by hero:** a rule-bearing habit ("Water") refuses manual ticks — no tick circle, a token-coloured Zap badge + live "900 / 2,000 ml", and a tap reveals "Satisfies itself from Health — water ≥ 2,000 ml"; rule-free habits (Wake/Meditate/No sugar) keep a real tap-tick that writes a `source:"manual"` log in place (the refusal boundary is enforced in the UI AND server-side, DB-proven). Gated: Sol code review 0 blocking + Sol D-041 design review → **1 mobile-blocking fix** (the ⚡ was a raw gold emoji → token-coloured lucide Zap) + glass-box coherence (the water goalpost is now one number, 2000 ml) + a scoped `habitId` existence check + a reduced-motion guard + a fuller capture-bar backdrop. `pnpm check` **133 tests** green, invariants clean, keyless; evidence in `.verify/screens/habits-*`. The shared P3a criterion (Health + Habits row grammar identical, satisfied-by refuses) now holds. Next: SAR-010 (Skills mastery lens).
- **SAR-010 — Skills mastery lens LANDED 2026-07-18 (D-044).** The third fan-out lens completes the demo spine: Skills (`skills`/`skill_sessions`/`skill_milestones`) render as a pure read-model (`core/domains/skills.ts`) + lens with per-skill mastery = summed session minutes (integer, correctly NOT the per-domain `cumulativeMinutes`), a track list, and a lens-local milestone drill whose **mastery counter is the unmistakable hero** (violet `128:30`) over the curriculum (token-coloured lucide ✓/▸/○) + a realistic session log. Gated: Sol code review 0 blocking + Sol D-041 design review → **2 mobile-blocking fixes** (dormant-card text failed AA in light → dim only decoration; the 44px back-chip law → padded chip + lucide ChevronLeft) + polish (dropped the frozen `:00` clock suffix, per-mode meter-rail visibility, removed a dev-ish source enum, redistributed implausible mega-sessions into believable ones). `pnpm check` **146 tests** green, invariants clean, keyless; evidence in `.verify/screens/skills-*`. **The demo-spine fan-out (SAR-008/009/010) is complete — all four domain lenses (Health, Money, Habits, Skills) are real and screenshot-verified.** The shared P3b criterion (drills push/return; the mastery counter is the hero) holds. Next: the F3→fan-out lane continues with SAR-011 (vision) → 012 (onboarding) → 013 (voice) → 014 (coach/game).
- **SAR-011 — Meal/receipt vision path LANDED 2026-07-18 (D-045).** The camera path now works keyless: the capture bar's camera button → a photo preview with a **meal/receipt type toggle** (Satvik's signed choice) → `parse-photo` route → `FakeVisionProvider` → the pure `core/capture/vision.ts` mapper → the **same** estimate-card loop → typed writes (meal → Health, receipt → Money batch, F5). **Invariant #1 made visible:** anything carrying photo evidence routes to pending (one routing rule) — a photographed receipt, even at explicit values, shows as confirm cards with an empty filed-strip and needs "Accept all", while explicit *text* still auto-files; the server re-route (D-040) re-enforces it, and the `f5-receipt` eval proves `wrongSilentWrites === 0`. The `CaptureSheet` refactor (a shared `ingestDraft`) is Sol-verified byte-for-byte identical for the text F3 path. Gated: Sol code review 0 blocking + Sol D-041 design review → 1 mobile-blocking fix (sheet `bg-canvas` → `--bg-raised` elevation, which upgrades both paths) + polish (44px toggle, label out of Fraunces, de-duped preview) + `parse-photo` type validation (400, ask-don't-invent) + test hygiene. `pnpm check` **155 tests** green, invariants clean, keyless; evidence in `.verify/screens/photo-*` (390 + desktop). Next: SAR-012 (onboarding + plan creation).
- **SAR-012 — Onboarding & real plan creation LANDED 2026-07-18 (D-046).** The production new-account path ships keyless, built in three gated passes. **Pass 1** — the routing gate (`app/(app)/layout.tsx` async → `/onboarding` unless the profile is `complete`; `SEED_STATE=fresh` lands onboarding, all other seed states carry a `complete` profile so the Today demo is untouched) + Phase A welcome + Phase B core (B1–B6, metric-int body conversion, min-1-domain, fast-tier fake voice-fill) + localStorage draft/resume. **Pass 2** — Phase C spine generation (a real deep-tier `generateObject` per selected domain through the gateway; `deriveSpine` is deterministic + **answer-derived**; keyless spines vary by answer; parallel + 8s budget + retry; StrictMode-safe) + Phase D confirm cards (domain-hued, glass-box Fraunces headers, editable stepper rows, swipe-drop, regenerate, the earned amber CTA) + **the ONE atomic replay-safe accept write** (profile → money/habits/skills/plan arcs+Day-1 items → progress/snapshots → `profile_gaps` → deterministic Day-1 coach note → flip `onboardingStatus:complete`; in-txn re-check = clean replay, intra-payload dup-name collapse, schema/timezone boundary guards, integer paise, nothing before the tap). **Pass 3** — E DETAIL (5 skippable sections resolving the accept-created gaps, integer-paise E5 recurring seeds, single-flight) → F theme (real per-theme previews, live-applied + persisted, app-mode-aware ring) → G landing (Today Day-1: coach first line, dismissible mic hint, plan populated from the spine) + the `seed_runs`-idempotent long-press demo gesture (local-mode-only, 404 otherwise). Gated per pass: Sol code review (0 CRITICAL/HIGH; the moat write is atomic/idempotent/integer-clean, with regression tests for the concurrency re-check, intra-payload dup-name, and StrictMode-hang bugs the review caught) + Sol D-041 design review of the rendered shots — Passes 1 & 2 GATE PASS after fix cycles; **Pass 3 closed at Satvik's call without the final screenshot re-review** (two residual polish items — Today section-eyebrow AA + the E-grid Done CTA height — fixed directly). `pnpm check` **192 tests** green, invariants + HARMONY clean, keyless; evidence in `.verify/screens/onboarding-*`. **Deferrals:** gap consumption → SAR-014; auth sign-in + authenticated demo-clone → SAR-021+; voice audio → SAR-013; E-section voice-fill parity → follow-up; the seed-route guard must assert `AUTH_PROVIDER=supabase` in SAR-021's deploy checklist. Next: SAR-013 (Voice PTT + transcript safety).
- **SAR-013 — Voice PTT + transcript safety LANDED 2026-07-18.** Codex accelerated the safe voice ingress: hold-to-record, tap-to-toggle, and keyboard PTT produce an ephemeral bounded browser clip; the `30_000` ms ceiling stops recording before upload and the framework-clean injected transcription service validates its duration before calling only `VoiceProvider`. Fake STT remains deterministic and keyless; no real adapter/key, audio persistence, outbox, schema, or migration was added. A failed provider/network call leaves the open audio draft retryable with **zero** typed rows, commit, XP, or plan effects. A successful STT result is visibly pinned and editable before the user explicitly parses it; only then does voice enter the unchanged F3 parse → route-by-confidence → typed-commit path, with server-owned voice/transcript metadata. Gated: Sol code review caught and fixed two omissions (voice tests now run in root `pnpm check`; keyboard is a real tap-to-toggle alternative) and the D-041 screenshot harness initially released into tap mode too quickly, then was corrected to prove the rendered recording, transcript, confirm, and retry states. `pnpm check` **197 tests**, build, invariant hook, and the final 390px Ember dark/light + desktop smoke design review pass keyless; evidence in `.verify/screens/voice-*`. Next: SAR-014 (coach engine, adaptations, and game mechanics).
- Codex accelerated the onboarding moat: the four domain spines are generated by ONE `generateObject` seam per domain through the provider-blind `LlmGateway` (op `onboarding-spine`), with a deterministic `deriveSpine` that visibly varies the plan by the user's B-answers (time budget, day-shape, goals, under-18) — so the "one flow, four outputs" wow runs **keyless** on the fake stack yet stays a real structured-LLM call when a key is flipped on, and the confirm cards demonstrably derive from the answers.
- **SAR-014 — Coach engine, adaptations + game mechanics LANDED 2026-07-18.** Codex accelerated the coach safety boundary: one framework-clean `CoachEngine` over exactly four typed `DomainSpec`s (no domain-specific orchestrators) now generates deterministic fake fast capture lines and deep daily/weekly notes from bounded typed evidence and open onboarding gaps. Staleness keys reuse fresh notes; the coach never closes a gap or invents data. Re-entry begins at **three elapsed local days**, produces one visible numeric-lightening proposal, and never silently changes Today. **Keep** creates an auditable `tool` commit with exact `planItem` snapshots; `adaptations.appliedCommitId` lets the five-minute latest-batch undo restore both the plan and the visible lifecycle atomically. **Revert** before Keep preserves the plan. Sol's two-pass review caught and fixed the grace-delayed threshold and repeated stacked reductions after Keep. Mastery, plan/evidence counts, and `adaptationSanityRate` derive as integer-safe read models. `pnpm check` (**205 tests**), build, invariants, and diff check pass keyless. No rendered surface changed; SAR-015 owns the Coach D-041 screenshot gate.
- **SAR-015 — Coach reading room + Stats/Journey views LANDED 2026-07-18.** Codex accelerated the product's reflection loop: one scoped server composition plus framework-clean readers projects typed CoachNotes, Adaptations, evidence, progress, plans, and Day-1 snapshots into a quiet Coach reading room, auditable Stats faces, and an evidence-first Journey rail—without a generic event log or new persistence. Adversarial review stopped seven behavioural defects before land: Sunday now generates daily and trailing-week briefs independently; weekly rendering and counts are evidence-window-bound; Coach replaces the capture dock; F6 has `View recap`; F9 shows semantic before/after/unit/reason; historic evidence is never relabelled Today; and the demo has a real multi-proof date. A final review also caught stale client state after Keep/Revert or typed gap answers; refreshed server data now syncs into the reading room. `pnpm check` (**213 tests**), build, invariant validation, and diff check pass keyless. **D-041 visual review accepted manually by Satvik**; generated Coach/Stats/Journey evidence is retained under `.verify/screens/`.
