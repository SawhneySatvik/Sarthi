# ~/.codex/build.config.toml  →  codex --profile build
# THE writer (D-028): Terra writes all code. High effort for the moat + ship work.
model = "gpt-5.6-terra"
model_reasoning_effort = "extra-high"
model_verbosity = "low"
sandbox_mode = "workspace-write"
