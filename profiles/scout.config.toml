# ~/.codex/scout.config.toml  →  codex --profile scout
# Fast, cheap, read-only exploration and doc verification.
model = "gpt-5.6-luna"
model_reasoning_effort = "extra-high"
model_verbosity = "medium"
sandbox_mode = "read-only"
